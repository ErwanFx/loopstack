# Article writing

Create one useful article from `keyword-brief`. If `review-decision` is present, address every cited defect. Preserve verified claims and return only the typed `article-draft`; never publish from this node.
