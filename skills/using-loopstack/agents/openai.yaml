interface:
  display_name: "Use Loopstack"
  short_description: "Route work through six fluid Loopstack phases"
  default_prompt: "Use $using-loopstack to resume or route this loop. Auto-chain completed non-blocked handoffs; stop only at real gates."
