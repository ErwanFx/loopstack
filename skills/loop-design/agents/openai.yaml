interface:
  display_name: "Design Loop"
  short_description: "Blueprint HTML (T→O→E→A→L→D) + YAML; owner approves before next skill"
  default_prompt: "Use $loop-design. For an initial design, build ai-loop-blueprint.html with architecture-diagram (Target→Observe→Evaluate→Act→Learn→Decide), include mandatory Hermes native learning in Learn, map runtime skills, write the YAML package, present the HTML, and wait for APPROVE before loop-storage-design. If loop-eric-review returned REVISE, patch only its exact defects, preserve approved components and storage, make the revision visible in HTML+YAML, wait for owner approval, then route back to loop-eric-review instead of repeating storage design."
