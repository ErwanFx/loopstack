interface:
  display_name: "Plan Loop"
  short_description: "Write the exact versioned build plan and approval gate"
  default_prompt: "Use $loop-plan to write a TDD implementation plan with exact mutations, hashes, tests, rollback, and a hard approval stop."
